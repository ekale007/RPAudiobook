# Optional — speeds up Hugging Face model download
HF_TOKEN=hf_...

# Optional — for KI prompt optimization (or set key in the UI)
OPENROUTER_API_KEY=sk-or-...
OPENROUTER_MODEL=google/gemini-2.5-flash-lite

# GPU: auto | cuda | cpu
SDXL_DEVICE=auto

# Server port (default 5125)
IMAGE_STUDIO_PORT=5125
